# Optional. Only needed to run the local asset-generation script.
# Get a token at https://replicate.com/account/api-tokens
# Put the real value in .env.local (gitignored) — never commit it.
REPLICATE_API_TOKEN=

# Never expose this token under a NEXT_PUBLIC_* variable name —
# those are bundled into the browser build and would leak it.
